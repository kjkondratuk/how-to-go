// Declare the namespace of this source code--the entrypoint is always "main"
package main

// Declare any modules you need to import, in this case a text formatter from the standard library
import "fmt"

// The main function signature will always be the same
func main() {

	// Write out "Hello, world!" to stdout
	fmt.Println("Hello, world!")
}
